import { Box, Typography } from "@mui/material";

const CommandPanel = () => {
  return (
    <Box id="CommandPanel" border="1px solid red" flex={1}>
      <Typography variant="body1">Quick Commands</Typography>
    </Box>
  );
};

export default CommandPanel;
